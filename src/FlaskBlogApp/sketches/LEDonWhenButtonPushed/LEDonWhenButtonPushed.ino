const int buttonPin = 8, ledPin = 2; bool isButtonPressed = false; void setup() { pinMode(buttonPin, INPUT); pinMode(ledPin, OUTPUT); digitalWrite(ledPin, LOW); } void loop() { bool buttonState = digitalRead(buttonPin); if (buttonState == HIGH) { digitalWrite(ledPin, HIGH); isButtonPressed = true; } else { if (isButtonPressed) { digitalWrite(ledPin, LOW); isButtonPressed = false; } } }